# Miscellaneous

This directory contains files that did not fit any other category including:

- A Hardware WatchDog configuration that checks if `/tmp/heartbeat` has been updated in the last 10 seconds or otherwise it reboots the system (takes 60 seconds to actually do something)